# 서울 아파트 매매 대시보드 — PRD

**버전** v1.0 · 2026-08-12 · GitHub Pages 정적 호스팅

---

## 1. 제품 개요

**한 문장 정의:** "내 예산으로 서울 어느 구까지 갈 수 있는지"를 최근 12개월 실거래 매매 72,459건으로 직접 따져보는 정적 대시보드.

**해결하는 문제:** 부동산 정보는 지역을 먼저 고르게 한다. 하지만 내집마련은 예산이 먼저 정해진다. 이 대시보드는 순서를 뒤집는다 — 예산을 넣으면 25개 구가 가능/경계/불가로 갈린다.

**이 제품이 아닌 것:** 투자 추천 도구가 아니다. 대출·금리·세금을 계산하지 않는다. 미래를 예측하지 않는다.

---

## 2. 페르소나 & 시나리오

**김윤지** — SeSAC 파이썬 수강생, 내집마련 실제 탐색 중. 포트폴리오에 올라가지만 **만든 동기는 포트폴리오가 아니다.**

- **S1.** 밤 11시, 8억 → 9억으로 바꿔본다. 가능 구가 6곳→11곳. **1억이 구 5개로 환산되는 경험.**
- **S2.** 노원구 상세로 들어가 동·단지까지 파고든다. 구 단위는 시작일 뿐.
- **S3.** 부모님께 "왜 강북이냐" 설명. 주장이 아니라 데이터로.

---

## 3. 핵심 원칙 (기능보다 우선)

- **R1. 헤드라인은 라벨이 아니라 발견한 사실.** "구별 시세 현황" ✗ → "8억이면 25개 구 중 6곳이 중위값 안쪽입니다" ✓. 빌드 시 `summary.json`의 `copy`에 생성, 하드코딩 금지.
- **R2. 한계를 옆에 붙인다.** 모든 수치 블록에 `n=표본수 · 기간 · 제외조건` 캡션 고정.
- **R3. 기본값이 곧 관점.** 예산 8억 / 면적 60~85㎡(국민평형) / 정렬 중위가 낮은 순.
- **R4. 집계 옆에 실제 거래 한 건.** "중위 7.12억"보다 "상계주공7단지 79.1㎡ 12층 2026-03 7.1억".
- **R5. 판정이지 조언이 아니다.** "추천"·"노려보세요" 금지. 산식 그대로 노출.
- **R6. 대시보드에서 자기 자랑 금지.** 기술 스택 배지는 about.html로.

---

## 4. 정보 구조

```
/
├── index.html          대시보드 (메인)
├── about.html          자기소개 (기존 index.html 이관)
├── assets/tokens.css   Montage 토큰 (공용)
├── assets/dashboard.css
├── assets/dashboard.js
├── data/json/summary.json      초기 로드 (~10KB)
├── data/json/gu/{slug}.json    25개, 지연 로드
└── code/build_data.py          CSV → JSON 빌드
```

- **해시 라우팅** `index.html#/gu/nowon`. 구 JSON은 메모리 캐시.
- 예산·면적대는 `localStorage` 저장 (URL에 개인 재무정보 금지).
- **파일명은 slug만** (한글 파일명 URL 인코딩·NFC/NFD 문제 방지).
- `pages/index.html`은 루트와 바이트 동일 중복 → 삭제.

---

## 5. 기능 요구사항

### P0
- **F1. 구 상세 — 동 랭킹** `1순위`. 중위가 오름차순, p25–p75 막대 + 예산선 오버레이. 구당 3~30개 동.
- **F2. 구 상세 — 단지 랭킹** `1순위`. 3건 이상 단지만(3,648개 = 거래 95.9% 커버). 30개씩 점진 렌더 + 검색.
- **F3. 예산 기준선 25개 구 판정** `시그니처`. §5.1 참조.
- **F4. `build_data.py`** — 표준 라이브러리만, **`csv.DictReader` 필수**, 60초 이내, assert 5종.
- **F5. 페이지 분리** — about.html 이관, tokens.css 추출.

### P1
- **F6.** 구 비교 표 (25행, 6열 정렬)
- **F7.** 12개월 흐름 (인라인 SVG 라인 + 거래량 막대)
- **F8.** 가격 분포 히스토그램 (1억 단위 31구간)
- **F9.** 면적대 전환 6종 (JSON 재요청 없이)

### P2
F10 구 비교 핀 / F11 예산 히스토리 / F12 딥링크 / F13 프린트

---

## 5.1 예산 기준선 — 시그니처 설계

**입력:** 숫자 입력(억, step 0.5, 3~30) + 슬라이더 + 프리셋 칩(6/8/10/12억). **디바운스 없이 즉시 반영** — 실시간 재배치 자체가 체험. 내부는 **만원 단위 정수**.

**판정 알고리즘:**
```
n < 30              → 표본부족
q50 <= B            → 가능
q25 <= B            → 경계
else                → 불가
affordShare = (B 이하 거래 수) / n
```

**백분위수:** nearest-rank `sorted[min(n-1, floor(n*p))]`. **`statistics.median` 금지** — 짝수 개 평균은 실재하지 않는 가격을 만들고 R4와 충돌.

| 등급 | 조건 | 색 | 카피 |
|---|---|---|---|
| 가능 | 중위 ≤ 예산 | `--status-positive` | 절반 이상의 거래가 예산 안쪽 |
| 경계 | 하위25% ≤ 예산 < 중위 | `--primary-normal` | 하위 25~50% 구간에서만 가능 |
| 불가 | 예산 < 하위25% | `--label-assist` | 하위 25%도 예산을 넘음 |
| 표본부족 | n < 30 | `--fill-strong` | 거래 {n}건뿐이라 판정하지 않음 |

빨강 금지 — "불가"는 경고가 아니라 사실.

**수용 테스트 (8억·60~85㎡ 실측 검증됨):**
- 가능 6곳: 도봉 5.95 / 중랑 6.88 / 강북 6.95 / 구로 6.97 / 노원 7.12 / 금천 7.50
- 경계 8곳: 관악 8.25 / 은평 8.50 / 강서 8.53 / 종로 8.70 / 성북 8.75 / 동대문 9.35 / 서대문 9.40 / 양천 9.90
- 불가 11곳: 영등포 11.92 … 강남 26.50

**예산별 회귀 케이스:**
| 예산 | 6억 | 7억 | **8억** | 9억 | 10억 | 12억 | 15억 |
|---|---|---|---|---|---|---|---|
| 가능 | 1 | 4 | **6** | 11 | 14 | 16 | 20 |
| 경계 | 4 | 5 | **8** | 5 | 3 | 4 | 3 |
| 불가 | 20 | 16 | **11** | 9 | 8 | 5 | 2 |

**8억→9억에서 6→11 점프** — S1의 "1억의 의미"가 데이터에 실재.

**가드 문구 (상시 노출):** `예산은 매매가 상한만을 뜻합니다. 대출 한도·금리·취득세·중개보수는 계산하지 않습니다.`

---

## 6. 데이터 파이프라인

### 원본 (검증 완료)
| 항목 | 값 |
|---|---|
| 전체 행 | 320,141 |
| **매매** | **72,459** (전세 129,649 / 월세 118,033) |
| 파싱 실패 | **0** (csv 모듈 기준) |
| 기간 | 2025-07 ~ 2026-06 (12개월) |
| (구,동) 조합 | 316 · (구,동,단지) 5,880 |
| `price` 단위 | 만원 |
| `price_per_pyeong` | **전용면적 기준** (오차 0.097%) |
| 매매 결측 | dong/complex/floor/ppyeong **0건** |

**면적대 분포:** ~60㎡ 21,461 / 60~85㎡ 24,101 / 85~102㎡ 17,519 / 102~135㎡ 7,203 / 135㎡~ 2,175

**극단값:** 삭제하지 않는다. 전부 실제 거래. 면적대 분리로 격리 + 히스토그램 30억+ 합산.

### JSON 스키마 요약
- `summary.json` — meta / seoul(stats·monthly) / gu[25](stats·hist) / **copy(R1 생성 문장)**
- `gu/{slug}.json` — stats / hist / monthly / **dongs** / **complexes**(rep 포함) / excluded / **samples(실거래 20건)**
- 모든 가격 **만원 정수**. `null` 대신 **키 생략**. `ensure_ascii=False, separators=(",",":")`.

### 용량 예산
| 항목 | 예상 | 상한 |
|---|---|---|
| summary.json | ~10KB | 60KB |
| 구 파일 최대 | ~40KB | 60KB |
| **총량** | **~660KB** | **1.5MB** |
| 초기 전송 | — | 250KB |

### 빌드 assert (전부 통과해야 기록)
```python
assert total_rows == 320141
assert sale_rows  == 72459   # split(",") 회귀 방어선
assert len(gu_index) == 25
assert dropped_rows == 0
assert all(size <= 60*1024 for gu_files)
```

---

## 7. 화면 스펙 요약

**홈:** Hero(예산 입력 + h1 "8억으로 서울 어디까지 갈 수 있을까" 실시간 변경) → S1 예산 판정 3열 → S2 면적대 세그먼트(sticky) → S3 구 비교 표 → S4 12개월 흐름 → S5 한계 섹션(항상 펼침)

**구 상세:** 헤더(판정 배지 + 산식 노출 + 4스탯) → 동 랭킹 → 단지 랭킹 → 실거래 20건 → 한계

**sticky 예산 컨트롤이 구 상세까지 따라온다. 화면 어디에도 예산이 안 보이는 곳이 없어야 한다.**

---

## 8. 비기능

- LCP ≤1.5s / 슬라이더 재렌더 ≤16ms / 구 상세 진입 ≤400ms (재진입 즉시)
- 반응형 4단계 (≤599 단일컬럼, 예산 컨트롤 하단 고정바)
- **접근성 AA:** 색만으로 등급 구분 금지(텍스트 병기), SVG `role=img`+`<title>`+대체 표, `aria-sort`, `aria-live=polite`는 **요약 문장만**(25개 칩 read 금지), `prefers-reduced-motion`
- 숫자 표기: 가격 `7.12억`(1억 미만 만원) / 건수 `7,190건` / 비율 `77.2%` / `Intl.NumberFormat('ko-KR')`
- 번들러·npm 금지. `<script type="module">` 순수 정적.

---

## 9. Non-goals

전세·월세 / 대출·LTV·DSR·세금 / 가격 예측 / 건축년도·세대수(**원본에 없음**) / 학군·교통(**원본에 없음**) / 지도 choropleth(성능 대비 이득 작음) / 실시간 갱신 / 계정 / 매물 검색 / 서울 외 / 다크모드 / 다국어 / 빌드 툴체인 / pandas

---

## 10. 주요 리스크

| # | 리스크 | 대응 |
|---|---|---|
| **R1** | **`split(",")` 파싱으로 471건 유실** (실제 발생함) | `csv.DictReader` 강제 + assert `sale_rows==72459` |
| R2 | 소형 평형 혼입 왜곡 (13㎡ 6,500만원 등 1,482건) | 기본 60~85㎡ 고정 |
| R3 | 표본 부족 구 오판 (종로 183건, 용산 306건) | n<30 미판정 / n<10 배지 |
| R4 | 투자 조언 오독 | 산식 노출, 추천 표현 금지, 금융 계산 배제 |
| R8 | 한글 파일명 인코딩 | slug 강제 |
| R11 | `gu_stats.py` 잘못된 수치(노원 4,643 vs 실제 7,190) 유입 | 경고 주석 + 미참조 |

---

## 11. 데이터 검증 정정 사항 `중요`

### A. `deal_type` 숫자 혼입 = 데이터 손상 아님, **파싱 버그**

원본은 깨지지 않았다. `csv.DictReader`로 읽으면 320,141행 전부 정상, 파싱 실패 0건.

원인: **단지명에 쉼표가 든 따옴표 필드**를 `split(",")`로 자르면 컬럼이 밀린다.
```
"현대1차(12,13,21,22,31,32,33동)"   ← 쉼표 6개
```
- 따옴표 필드 포함 행: 3,248행 (그중 **매매 471행**)
- 손실 분포: 강서 255, 강남 71, 중랑 54, 서대문 34, 구로 18, 성북 11 …

**요구사항이 반대로 뒤집혀야 한다.** 필터링하면 압구정 현대·등촌1,2단지주공·대치우성 같은 대단지가 통째로 사라져 편향 발생. **모든 행을 살리기 위해 `csv` 모듈을 쓴다**가 올바른 요구사항.

### B. 결측치 없음 — 매매 72,459행에서 dong/complex/floor/ppyeong 결측 0건

### C. 동 분포 극단적 — 관악·금천·양천 3개 vs 영등포 30개. UI가 3~30행 모두 감당해야 함

### D. `price_per_pyeong`은 전용면적 기준 — 분양 광고 공급면적 평당가와 다름을 UI에 명시

### E. `pages/index.html` = 루트와 바이트 동일 중복 → 삭제

---

## 12. 릴리스 순서

| 단계 | 산출물 | 검증 |
|---|---|---|
| 1 | `build_data.py` → JSON 26개 | assert 통과, ≤1.5MB |
| 2 | tokens.css + about.html 이관 | 픽셀 동일 |
| 3 | index.html + **F3 예산 판정** | 8억 배치 정확히 일치 |
| 4 | **F1 동 + F2 단지 랭킹** | 25개 구, 강남 244단지, 쉼표 단지명 |
| 5 | F6 표 + F9 면적대 | 전환 시 JSON 재요청 0 |
| 6 | F7 차트 + F8 히스토그램 + 한계 | SVG 대체 표 |
| 7 | 접근성·반응형·에러 | 키보드 단독 전체 조작 |

**3↔4 사이 사용자 리뷰 필수** — 예산 판정 화면이 제품 성패를 결정.

---

## 13. 성공 기준

1. 처음 보는 사람이 5초 안에 **"이 사람 집 사려고 만들었구나"**를 안다
2. 예산 숫자를 최소 두 번 바꿔본다 (8↔9억 극적 변화가 유도)
3. 구 상세까지 최소 한 번 들어간다
4. "몇 건 기준이냐"는 질문에 화면이 이미 답하고 있다
5. "뭘 알 수 있냐"보다 **"뭘 알 수 없냐"**에 먼저 답한다
